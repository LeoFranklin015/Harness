# Secrets — nothing in here ever reaches a commit
.env
.env.*
!.env.example
*.key
*.pem
*.p12
secrets/
keystore/
deployer.json
# Notes, write-ups and dropped files stay local, all of them. Nothing here
# goes to a remote unless it is put there deliberately.
# docs/
planning/
.orca/
CONTEXT.md
PRODUCT.md
Project-details.md
SPEC.md
*.enc.local

# Ledger Key Ring / wallet-cli local state (member private key, trustchain session)
session.yaml
.ledger-wallet-cli/
ledger-wallet-cli/

# Tailscale node state — node identity, auth keys
tailscaled.state
tailscale.state
var/lib/tailscale/
*.authkey

# Node
node_modules/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
.pnpm-store/

# Foundry (broadcast/ is deliberately kept — deployment records are worth committing)
cache/
cache_forge/
artifacts/

# Go
*.exe
*.test
*.out
vendor/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Agent tooling — installed, not authored
.claude/
.agents/
skills-lock.json

# Scratch
tmp/
scratch/
*.local
x402/.relayer
x402/.agent-root

# Runner host keys: generated per container, never committed.
runner/ssh_host_ed25519_key
runner/ssh_host_ed25519_key.pub

# Built by dns/run-dns.sh, copied into the image. Not source.
dns/harness-dns

# Grant records live in the metadata store, not the repo: they are every
# Agent's key, ceiling and window, and a clone should not come with a roster.
x402/grants/
web/p.mjs
web/shot.mjs
docs/diagrams/Screenshot*
